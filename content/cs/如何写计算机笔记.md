---
title: 如何写计算机笔记
tags:
  - cs
---
新笔记可以丢进 `inbox/`，写完再挪到 `cs/` 或 `math/`。不想公开的内容放 `private/`。
代码用语言围栏：

```python
def greet(name: str) -> str:
    return f"hello, {name}"
```

相关概念用双链，例如 [[math/LaTeX示例]]。